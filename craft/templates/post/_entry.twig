{% extends "_layout" %}
{% import '_macros/_import.twig' as sections %}

{% block content %}
    {{ sections.story({
        entry: entry,
        index: 'style1'
    }) }}

    {% for bodyBlock in entry.bodyBlocks %}
        {{ attribute(sections, bodyBlock.type().handle, [bodyBlock]) }}
    {% endfor %}
{% endblock %}

{% block footer %}
    {# {% if pageInfo.totalPages > 1 %}
        {{ sections.pagination(pageInfo) }}
    {% endif %} #}
{% endblock %}